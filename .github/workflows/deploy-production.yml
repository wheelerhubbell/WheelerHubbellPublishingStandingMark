name: Repository production source
on:
  workflow_dispatch:
jobs:
  no-op:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Production deployment is owned by the connected Netlify project; no duplicate deploy or authority mutation runs from GitHub Actions.'
