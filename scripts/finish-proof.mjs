import {readFile,writeFile} from 'node:fs/promises';import assert from 'node:assert/strict';import {sourceManifest} from './source-manifest.mjs';
const E='evidence/public-v1',load=async p=>JSON.parse(await readFile(p,'utf8'));const core=await load(E+'/core-verification.json'),postgres=await load(E+'/postgresql/report.json'),source=await sourceManifest();
assert.equal(core.source_manifest_sha256,source.sha256,'SOURCE_CHANGED_AFTER_VERIFICATION');assert.equal(postgres.source_manifest_sha256,source.sha256,'POSTGRES_TESTED_DIFFERENT_SOURCE');assert.equal(postgres.all_passed,true);assert.equal(postgres.environment,'TEST');assert.equal(postgres.production_database_accessed,false);assert.equal(postgres.live_payment_executed,false);assert.equal(core.test_counts.pass,core.test_counts.tests);for(const k of ['fail','skipped','cancelled','todo'])assert.equal(core.test_counts[k],0);assert.equal(core.cold_agent_report['PROPAGATION-PROVEN'],'TEST');
const report={...core,postgresql_integration_tested:true,postgresql:postgres,complete_thread_proof:true,evidence_states:{DESIGNED:true,IMPLEMENTED:true,VERIFIED:true,'PROPAGATION-PROVEN':'TEST',DEPLOYED:false,LIVE:false,SOLD:false,ISSUED:false},completed_at:new Date().toISOString(),completion_scope:'First public v1 cryptographic contract and complete inherited plus bounded TEST propagation proof. No production assertion.'};
await writeFile(E+'/verification.json',JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));
