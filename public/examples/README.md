These are actual bounded TEST wire examples. They are not LIVE Marks, actual sales, production credentials or persistent public service locations. The Mark records its original ephemeral local TEST endpoint; the cold test creates a fresh running instance for traversal. Historical verification is possible using the embedded public TEST root commitment with --allow-test; current standing is never inferred from the static example.
